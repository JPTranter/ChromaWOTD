# ChromaWOTD partition table (ESP32-S3, 8 MB flash).
#
# Baseline: the framework's `default_8MB.csv` — the table this board ACTUALLY builds
# with. The board definition (`espressif32/boards/seeed_xiao_esp32s3.json`) sets
# `"partitions": "default_8MB.csv"`, whose app0 size of 0x330000 is exactly the
# 3342336-byte app slot PlatformIO reports. (Note: NOT `default.csv` — that one has a
# 0x140000 app0 and different app1/spiffs offsets.)
#
# The `nvs`, `otadata` and `app0` offsets are deliberately kept IDENTICAL to that
# baseline, which is what makes this change low-risk:
#   * `tools/merge_firmware.py`'s LAYOUT still holds (no code change there),
#   * an app-only flash at 0x10000 still targets app0,
#   * the bootloader at 0x0 is untouched,
#   * so the update is `partitions.bin` @ 0x8000 + `firmware.bin` @ 0x10000, not a
#     full merged image at 0x0.
#
# WHY `nvs_cfg` EXISTS (BUG-06 / LESSONS §45)
# The Arduino core's initArduino() calls nvs_flash_init() and, when that returns
# ESP_ERR_NVS_NO_FREE_PAGES, erases a whole partition:
#
#   // cores/esp32/esp32-hal-misc.c:~250
#   if (err == ESP_ERR_NVS_NO_FREE_PAGES || err == ESP_ERR_NVS_NEW_VERSION_FOUND) {
#       const esp_partition_t* partition = esp_partition_find_first(
#           ESP_PARTITION_TYPE_DATA, ESP_PARTITION_SUBTYPE_DATA_NVS, NULL);
#       esp_partition_erase_range(partition, 0, partition->size);   // wipes EVERYTHING
#   }
#
# That 20 KB `nvs` partition is SHARED with the WiFi/BLE/DHCP stack, so when the WiFi
# state fills it, our stored credentials were destroyed along with it and the device
# silently re-offered its setup portal. Giving our configuration its own partition
# means that erase can no longer reach it.
#
# ORDERING IS LOAD-BEARING: `nvs` MUST remain the FIRST `data, nvs` entry in this
# table. The core locates the partition to erase with a NULL label, i.e. the first
# match by subtype. Move `nvs_cfg` above `nvs` and the core would erase `nvs_cfg`.
#
# Space comes from `spiffs` (unused — this project has no filesystem code) and
# `app1` (the OTA slot, unused — there is no OTA).
#
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x330000,
app1,     app,  ota_1,   0x340000,0x330000,
nvs_cfg,  data, nvs,     0x670000,0x4000,
spiffs,   data, spiffs,  0x674000,0x17C000,
coredump, data, coredump,0x7F0000,0x10000,
